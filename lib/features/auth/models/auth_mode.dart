/// Which of the two auth flows [AuthScreen] is rendering — the copy
/// and the cross-link at the bottom differ; the three provider
/// buttons behave identically either way.
enum AuthMode { signIn, signUp }
